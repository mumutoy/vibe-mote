# VibeMote · Google TV 遥控器 → Windows 输入设备

> 🌐 **中文** | [English](README.en.md)

把 **Google Chromecast / Google TV 蓝牙遥控器**变成 Windows 上的"说话 + 快捷键"设备：
**按住遥控器说话 → 遥控器麦克风 → 虚拟声卡 → 输入法出字**；14 个按键随便映射，
**上下=滚轮、左右=切会话、YouTube / NETFLIX = 审批的「同意 / 拒绝」**。

**下载 → 解压 → 双击 `启动遥控器.vbs`**，没有第四步（离线包自带 Python，目标机器不需要装任何东西）。

> ⬇️ **[下载最新版](releases/latest)**
> · `vibe-mote-offline-v1.zip`（约 61MB，**自带 Python + 完整功能**，解压就能跑 ← 推荐）
> · `vibe-mote-offline-voice-v1.zip`（约 14MB，**只语音、不含 frida** ← 怕杀软 / 公司电脑选它）
> · `vibe-mote-v1.zip`（约 900KB，目标机器已装 Python 3.10+ 时用）
> 更新日志 → [`CHANGELOG.md`](CHANGELOG.md) ·
> 让 AI 帮你装 → 把仓库地址和 [`AGENTS.md`](AGENTS.md) 一起丢给它

> ⚠️ **下载前先说清楚（30 秒版）**
> **语音功能**（按住说话出字）不需要管理员、不注入任何东西，前面那几个包都能用。
> **按键映射**（方向键/OK/音量）是靠**往系统的蓝牙 HID 驱动宿主里注入一个模块**实现的：要**一次**管理员权限
> （点过「一键优化」后不再弹），**可能被杀软误报/静默隔离**，少数带**内核反作弊的游戏**可能因此起不来，
> **公司/学校电脑**可能直接被策略拦掉。
> → 只要语音：下 `-voice` 包（不含 frida，包体也小），**不用点**任何「安装按键映射依赖」。
> → 要按键映射：完整包已经带了 frida，点一下界面的「安装按键映射依赖」即可；`-voice` 包之后再补 frida 有两条路 ——
> **① 联网**：点同一个按钮（自动走国内镜像，下载约 47MB）；**② 不联网**：把 frida 的
> `frida-*-win_amd64.whl` 丢进程序目录的 `wheels\` 文件夹，再点那个按钮（会优先用包内文件）。
> 两种方式装完首次挂载都会要**一次**管理员（或先点「一键优化」）。
> 完整清单见 [第五节第 5 小节](#5-已知的限制与风险宁可提前说不藏在角落里)。

首页就是一张「**首次准备**」清单 —— 五项该做什么、必需还是可选、当前什么状态、按钮在哪，
全摆在一屏里；从上往下点一遍就能用。

| 遥控器按键 | 默认效果 |
|---|---|
| **语音键**（带话筒那个） | **按住说话** —— 遥控器麦克风 → 虚拟声卡 → 输入法出字 |
| 上 / 下 | 鼠标滚轮上 / 下（**按住不放连续滚**，轻点滚一格） |
| 左 / 右 | 切换上一个 / 下一个会话（预设可改） |
| OK / 返回 / 主页 / 电源 / 输入源 / 静音 / YouTube / NETFLIX / 音量± | 网页界面里随便改 |
| YouTube / NETFLIX | 审批的 **同意 / 拒绝**（`Enter` / `Esc`） |

网页控制台（`127.0.0.1:8787`）：状态 · 按键映射（遥控器实物图）· 语音 · 设置 · 日志。
支持**预设方案**：选「目标应用（WorkBuddy / Codex / 通用）+ 输入法（千问 / 搜狗 / 微信 / Windows 自带）」，一键配好整套键位。

改键就在这一页：左边点遥控器上的任意键，右边改它发什么；预设方案一键配好一整套，
不满意可以「恢复推荐配置」，也能导出 / 导入。

---

> 🌐 **中英双语**：界面右上角可切换（首次打开按浏览器语言自动选，检测不出来就用中文），
> 英文文档见 [`README.en.md`](README.en.md)、[`说明.en.md`](说明.en.md) 与 [`AGENTS.en.md`](AGENTS.en.md)。
> 后端产生的主流程消息（依赖说明、预设方案、清理清单、环境体检）也是双语的；
> 底层诊断流水（音频帧、ATVV、frida 挂载细节）保持中文，那些是排障用的技术流水。

## 一、这个版本干净在哪

- ✅ **不装驱动**（没有虚拟 HID、没有内核过滤驱动、不碰 HidHide）
- ✅ **语音部分不需要管理员权限**。按键映射（可选）要**一次**提权 —— Frida 得往蓝牙 HID 驱动宿主里注入、
  并注册一个临时助手服务；点一次「**一键优化**」之后由最高权限计划任务代劳，**之后不再弹**。
  另外就是装虚拟声卡时那一次 UAC（那是声卡安装器自己的要求）。细节见第五节第 5 小节
- ✅ **不弹黑框**：单进程 + 全程隐藏窗口启动，开机自启也一个窗口都不闪
- ✅ **不开测试模式、不关 Secure Boot、不改系统文件**
- ✅ **界面只监听 `127.0.0.1`**，不触发防火墙弹窗
- ✅ **代码极少**：9 个源文件、**第三方依赖只有 3 个**（连 numpy 都不需要）

---

## 二、依赖清单（要装什么、什么版本）

### 运行环境

| 项 | 要求 | 说明 |
|---|---|---|
| 操作系统 | **Windows 10 / 11 x64** | 用到了 WinRT 蓝牙 API 与 SendInput |
| Python | **3.10 或更高** | 官网安装包，安装时**务必勾选 `Add python.exe to PATH`** |
| 虚拟声卡 | **VB-CABLE**（VB-Audio Virtual Cable） | 语音必备前置。装它需要一次管理员权限，**装完建议重启一次** |

> 用**离线全量包**的话，Python 和下面这些包都不需要你管，包里已经装好了。

### Python 包（`requirements.txt`）

| 包 | 版本 | 用途 |
|---|---|---|
| `bleak` | **3.0.2** | 蓝牙 GATT：语音键状态通道 + 遥控器麦克风音频 |
| `sounddevice` | **0.5.6** | 音频输出/输入（PortAudio 绑定），把遥控器麦克风送进虚拟声卡 |
| `cffi` | ≥1.16 | `sounddevice` 的依赖，自动带上 |

### 按键映射依赖（`requirements-keys.txt`，**可选**）

| 包 | 版本 | 用途 |
|---|---|---|
| `frida` | ≥17.18 <18 | **按键映射必需**（约 130MB）：注入蓝牙驱动宿主读 HID 报文。装在包内 `.venv`，用完即走 |

> **不装 frida 也完全能用** —— 语音键走 ATVV 通道，不依赖它。只是方向键/OK 那些映射不了。
> 装 frida 时首次挂载会要一次 UAC（Frida 给自己装助手）；跑一次本项目的「一键优化」后，
> 自启改成最高权限计划任务，**之后一次都不会再弹**。

### 明确**不需要**的东西（别被老教程带偏）

❌ WinUHid / 虚拟 HID 驱动　❌ 测试签名 / 关闭 Secure Boot　❌ HidHide　❌ Interception 驱动　❌ 管理员常驻权限

---

## 三、安装

### 路线 0：最省事（全新用户就两步）

```
① 双击「启动遥控器.vbs」   → 界面自己打开（没有黑框）
② 顶部横幅点「全部安装」   → 装完点「重启客户端」→ 完事
```

**全程不用敲一行命令**，装依赖也不用管理员权限（Frida 的**首次挂载**要一次提权，见第五节第 5 小节）。
程序会自动挑国内镜像下载
（腾讯云 → 中科大 → 阿里云 → 华为云 → 官方），装到程序目录里的 `.venv`，
不污染系统 Python。

> 只有 Python 需要你先自己装好（[python.org](https://www.python.org/downloads/)，
> 安装时**务必勾选 `Add python.exe to PATH`**）。
>
> **完全不想装 Python？**（或者要拷 U 盘给别人的机器用）→ 用下面的「路线 D 离线全量包」，
> 它自带 Python，什么都没装的电脑双击就能跑。

第一次打开界面时，横幅会直接告诉你缺什么、点哪个按钮，装依赖的**进度会自己跳到「设置 → 诊断」并滚到眼前**。

### 路线 A：手动（3 步）

```bat
:: 1) 进程序目录，建虚拟环境（不污染系统 Python，也不需要管理员）
python -m venv .venv
.venv\Scripts\python -m pip install -r requirements.txt

:: 2) 按键映射才需要（约 130MB，可选）
.venv\Scripts\python -m pip install -r requirements-keys.txt

:: 3) 启动
双击 启动遥控器.vbs
```

界面打开后点「安装基础依赖 / 安装按键映射依赖」也一样 —— **程序会自动轮询国内镜像**。

### 路线 B：让 AI 帮你装

把**这个仓库的地址**和下面这段话一起发给你常用的 AI（ChatGPT / Claude / 豆包 / WorkBuddy 都行）：

> 请读 `<仓库地址>` 里的 `AGENTS.md`，按它的步骤在我的 Windows 电脑上装好这个遥控器工具。
> 我的系统是 Windows 11。遇到不确定的地方先问我，不要自己装驱动或改系统设置。

`AGENTS.md` 是给 AI 看的执行手册：精确步骤、验证命令、失败排查表、禁止事项。
它照着做就行，不需要用户懂 Python。

### 路线 C：离线 / U 盘部署（没网或不想联网）

在一台**能上网**的机器上把依赖下成 wheel 包，塞进 `wheels\` 再整个拷走：

```bat
python -m pip download -r requirements.txt -d wheels
python -m pip download -r requirements-keys.txt -d wheels    :: 可选，约 130MB
```

程序**检测到 `wheels\` 目录就会优先用它**（`--no-index --find-links`），全程不联网。
整个包本体不到 1MB。
> 这条路仍然要求目标机器**装了 Python**（wheels 只是包，不是解释器）。

### 路线 D：离线全量包（没装 Python 也能用 / 拷 U 盘给别人）

`vibe-mote-offline-v1.zip`（约 61MB）**自带 Python 运行时 + 已装好的依赖**：

```
对方机器上：解压 → 双击「启动遥控器.vbs」→ 直接用
（不需要装 Python、不需要联网；按键映射仍要那一次提权，见第五节第 5 小节）
```

包里装着什么（以及为什么这么大）：

| 组成 | 大小 | 说明 |
|---|---|---|
| Python 3.13 官方 **embeddable** 包 | 10.4MB | 官方专门为"塞进应用里直接跑"提供的发行版 |
| bleak / sounddevice / cffi / winrt-* | 约 5MB | 语音必需 |
| frida | 47.3MB | 按键映射必需（这是包体的大头） |

> 界面上那两个「安装依赖」按钮在离线包上会直接告诉你"依赖已内置、不用装"。

> 📌 **内置的 `runtime\` 属于程序本体，清理工具不会删它。**
> 因为界面上的「全部安装」装的是 pip 包，**装不回**一个 Python 发行版 ——
> 删了就再也回不到"免安装"状态了。所以清理清单里干脆不提供这个选项。

---

## 四、第一次使用要做的 3 件事

**界面首页顶部有一张「首次准备」清单**，把这 5 项的状态和按钮都摆出来了，
照着从上到下点一遍就行（不用去「设置」页里翻）：

| 清单项 | 必需？ | 一行说明 |
|---|---|---|
| 运行依赖 | **必需** | bleak / sounddevice（语音）+ frida（按键映射）。点「全部安装」 |
| 虚拟声卡 | **必需** | 语音的物理前提。点「一键安装」 |
| 千问「允许注入」 | 可选 | 用千问输入法才需要；程序**首次启动会自动打开** |
| 开机自启 | 可选 | 一键开关，不需要管理员（想连 Frida 的 UAC 也一起去掉，就再点「一键优化」，那一下要管理员） |
| 一键优化（免 UAC） | 可选 | 做了之后 Frida 不再弹提权框 |

下面是手动的对应步骤：

1. **配对遥控器**：Windows 设置 → 蓝牙和其他设备 → 添加设备 → 同时按住遥控器 **返回 + 主页** 进配对模式
2. **装 VB-CABLE**：界面首页清单或「设置 → **虚拟声卡**」那张卡上点**一键安装** ——
   包内带了官方安装器就直接装，没带会先从官网下载（约 1.3MB）。
   - ⚠ **会弹一次 UAC**（那是声卡安装器自己的要求，与本程序无关），
     然后**在它自己的窗口里点一下「Install Driver」**，最后按 VB-Audio 的官方要求**重启一次系统**。
   - **首次启动时程序会自动帮你把这一步拉起来**（只自动试一次，你拒了 UAC 就不会再烦你）。
     **开机自启拉起的那一份不会弹**（自启命令带 `--boot`）：开机时你不在电脑前，弹了也没人点，
     而且会变成每次开机都弹一次。
   - 官方安装器**不提供静默安装参数**（没有 `-i`/`-s`/`/S` 这类开关），所以"自动"能省掉的只是
     "自己去找那个按钮"，那一下确认和重启省不掉。
3. **输入法里选麦克风**：选 **`CABLE Output (VB-Audio Virtual Cable)`**
   （只改输入法这一处，系统默认录音设备保持真实麦克风）

> **千问输入法用户不用做任何事**：千问默认会丢弃程序注入的按键，所以程序在**首次启动时
> 会自动帮你打开**「允许注入」开关（写用户环境变量、重启一次千问进程，不需要管理员）。
> 这个开关随时能在「语音」页手动开关 —— 你在「清理」里删过它的话，程序会记住、
> 不再自动加回来，想恢复点那个按钮就行。

---

## 五、它到底会动你电脑的什么（完整清单）

这是问得最多的一个问题，所以把话说全 —— **程序本体是纯绿色部署**：不装驱动、不写系统目录、
不需要管理员常驻（**按键映射要一次提权，以及它带来的限制/风险，单独写在第 5 小节**）。
下面是它**全部**会碰的东西，一项不漏：

### 1. 写在程序自己的目录里（删掉整个目录就没了）

`config.json`（配置）· `client.log`（日志）· `uninstall.log` · `qwen_optout`（你删过千问开关的标记）
· `cable_autotried`（自动装过声卡的标记）· `_ready.txt` · `_deferred_result.txt` · `_uninstall_result.json`

### 2. 系统里的设置改动 —— **每一项都是你自己点出来的，且都能一键撤销**

| 动什么 | 什么时候会动 | 怎么撤 |
|---|---|---|
| `HKCU\...\CurrentVersion\Run\VibeMote` | **只有**你打开「开机自启」 | 界面关掉，或清理清单里勾那一项 |
| 最高权限计划任务 `VibeMote` | **只有**你点「一键优化」 | 清理清单里勾「取消开机自启（计划任务）」 |
| `HKCU\Environment\QIANWEN_IME_UTILITY_VOICE_HOOK_ALLOW_INJECTED=1` | 装了千问输入法时自动写（也可以不写） | 界面按钮，或清理清单里勾那一项 |
| Frida 的临时助手服务 `frida-<pid>-x86*` | **Frida 自己**在挂载时注册的 | 清理清单里那一项；点过「一键优化」之后不再需要 |

**不做的事**（写下来是为了让你放心）：不装驱动、不碰 `System32` / `Program Files`、
不改输入法在注册表里的配置、不开测试模式、不动 Secure Boot、不关 Defender、不常驻管理员。
联网只有两处：你自己点「安装依赖」，或点「安装虚拟声卡」而包内没带安装器时（去 VB-Audio 官网下）。

### 3. 唯一会动到系统深处的两件事：**都由第三方安装器完成，程序只是替你启动它**

| 东西 | 谁装的 | 需要什么 |
|---|---|---|
| **VB-CABLE** 虚拟声卡（内核音频驱动） | **VB-Audio 自己的安装器** | 一次 UAC + 重启 |
| **Frida** 的助手进程 | Frida 自己的机制 | 首次挂载一次 UAC |

> ⚠ **必须提前知道的一件事：VB-CABLE 官方的卸载器卸不干净。**
> 点「Remove Driver」并重启之后，驱动可能**仍然存在**（设备管理器里状态正常、音频端点全部重新出现）。
> 原因是它留下了 DriverStore 里的驱动包、`C:\Windows\inf\oem634.inf` 和几个服务键，
> Windows 重启时照着又装了回去。
> 想**真正**删掉，得再跑一次管理员命令：
> ```
> pnputil /delete-driver oem634.inf /uninstall /force
> ```
> （`oem634.inf` 这个编号每台机器可能不同，用 `pnputil /enum-drivers` 找
> `Original Name: vbmmecable64_win10.inf` 那一条。）
> 这是 VB-Audio 卸载器自己的行为，与本程序无关 —— 但界面会照实说明，不会假装删干净了。

### 4. 会不会把别人的电脑弄坏？

不会。它没有内核驱动、不注册自己的常驻服务（Frida 挂载时会临时注册一个**助手服务**，点过「一键优化」
之后连这个都不需要）、不写系统目录，最坏情况只是"某个功能不工作 + 一条日志说明为什么"。
真正需要小心的两点已经在上面写明了：VB-CABLE 的安装/卸载要 UAC + 重启（那是它的要求），
以及清理时**默认什么都不勾** —— 只有你自己勾了才会删东西。

> 另外提醒：**同一时间只能有一个客户端连这个遥控器**（遥控器只给第一个客户端完整的服务表）。
> 如果你机器上还跑着别的同类工具，本程序会报「缺少 ATVV 特征」。先关掉其它客户端再试。

### 5. 已知的限制与风险（宁可提前说，不藏在角落里）

**按键映射（方向键/OK/音量那些）和语音是两套东西，代价也不一样：**

| | 语音（遥控器说话 → 出字） | 按键映射（14 个键 → 键盘/鼠标） |
|---|---|---|
| 需要管理员吗 | **完全不需要** | **需要一次提权**：Frida 要往蓝牙 HID 驱动宿主里注入，并注册一个临时服务 |
| 靠什么实现 | 普通蓝牙 GATT 连接 | 在**内存里**钩住那个驱动宿主（重启就没了），并把**被映射那几个键**的原生动作抹掉 |
| 不装 frida 会怎样 | 照常工作 | 这个功能不可用，其余一切正常 |

- **爆炸半径**：注入的是**蓝牙 HID 驱动宿主**，它同时服务你其他的蓝牙键鼠。我们只改被映射那几个键的
  报文，但万一这个宿主崩了，**蓝牙键鼠会一起掉线**（实测没遇到过；真发生就重启客户端或拔插一下适配器）。
- **依赖未公开接口**：按键映射用的是 Windows 蓝牙 HID 驱动的一个**未公开 IOCTL** 和一个注册表路径，
  并锁定 `frida >=17.18,<18`。Windows 或 frida 更新后它**可能静默失效** —— 失效时程序会优雅降级：
  语音照常，按键映射停用并在日志里说明。
- **杀软可能误报/误隔离**：frida 的模块名（`frida-agent.dll`、`frida-helper-*.exe`）在多家 AV 的特征库里。
  离线包约 61MB，其中 frida 约 47MB —— 被静默隔离的话，解压出来"按键映射"就是坏的（用户会以为是我们 bug）。
  症状：界面「安装按键映射依赖」后 `keys.ready` 一直 false、或日志说找不到 frida。
  处理：把程序目录加进 AV 白名单，或重新装一次依赖。我们**不会**让你关 Defender、开测试模式。
- **受保护的游戏**：往 SYSTEM 进程里注入这件事，行为特征和作弊器加载器同类。少数带内核反作弊的游戏
  可能因此起不来或报错 —— **玩游戏前把客户端退出**（或至少别用按键映射）是最省事的做法。
- **许可**：frida 是 wxWindows 3.1（LGPL-2.1+ 例外），随包分发合规，细节见
  [`THIRD-PARTY-NOTICES.md`](THIRD-PARTY-NOTICES.md)。

---

## 六、下载源（国内没 VPN 也能装）

程序内置**多源自动轮询**，一个失败就换下一个，顺序是：

```
包内 wheels（纯离线）→ 腾讯云 → 中科大 USTC → 阿里云 → 华为云 → 清华 TUNA → 官方 PyPI
```

正常情况下第一个源就能装完，不需要你操心。

> 两点提示：
> · **清华 TUNA 会返回空索引**（`from versions: none`），所以排在最后，别手动指定它；
> · **没有收录网易 163**：它的 PyPI 镜像早已停止同步，用它会装出老版本。
> 想换源：改 `app.py` 顶部的 `PIP_MIRRORS` 表。

---

## 七、出问题

界面「设置 → **环境体检**」会逐项告诉你哪一层断了（依赖 / 声卡 / 蓝牙 / 权限 / 自启 / 千问开关）。
「日志」页是实时流，报 bug 时把那段贴出来就够了。

| 现象 | 原因 | 解决 |
|---|---|---|
| 日志刷 `连接超时` | 遥控器在休眠 | **按一下遥控器任意键**，3 秒内自动连上 |
| 日志刷 `缺少 ATVV 特征` | 有另一个客户端占着蓝牙通道 | 关掉其它客户端再点「重启」 |
| 按语音键没反应 | 千问的「允许注入」没开 | 「语音」页点一下那个按钮 |
| 刚开机没声音 | 音频服务/声卡比程序晚就绪 | **等 5~10 秒**（程序每 5 秒重试，会自己接上） |
| 装虚拟声卡时报 `Unknown Error 1072` | 那个服务停在「待删除」状态（之前清残留只删了服务、没先停驱动）—— 它提示"请用管理员运行"是误导，你本来就是管理员 | **重启一次**，再点「一键安装虚拟声卡」；还不行就把 `vbcable\VBCABLE_Setup_x64.exe` 右键「以管理员身份运行」点 Install Driver |

程序会把启动日志写到同目录的 `client.log`；启动失败时启动器会弹提示并自动打开它。

---

## 八、卸载

两步，顺序别反：

1. 界面「设置 → 清理 / 卸载」点「选『推荐清理』」→ 开始清理（断开机自启 + 清 frida 残留服务 + 删依赖）；
2. 然后**直接删掉整个程序目录**。

程序是纯绿色部署（没有安装器、不写系统目录），第 ② 步之后系统里不会有任何残留。

**虚拟声卡不在这两步里，要单独说一句**：清理清单里有一项
「卸载虚拟声卡（VB-CABLE）」，但**默认不勾、也不属于「推荐清理」** —— 理由是
它是**系统级音频驱动**，OBS / 变声器 / 直播工具可能也在用它。真要卸的话：

- 勾上它 → 清理时会弹一次 UAC 并**启动官方的卸载器**，
  你还要在它窗口里点一下「**Remove Driver**」；
- 按 VB-Audio 的官方要求，**卸完必须重启一次**系统才算卸完。
- 所以它是清单里唯一一个"点完还要人动手 + 重启"的项，日志里都写明了。

**装/卸卡住了怎么办**（注册表里显示装了、设备却枚举不出来，点 Install Driver 没反应）：
清理清单里还有一项「**清除虚拟声卡残留（注册表 / 驱动包）**」，默认也不勾。
它只删 VB-CABLE 自己的那几处（两个服务键、`HKLM\SOFTWARE\VB-Audio\Cable`、
「应用和功能」里那条、DriverStore 里的驱动包、`C:\Program Files\VB\CABLE`），
**同属 VB-Audio 的 Voicemeeter 的键和目录一个都不会碰**。清完**重启一次**，再点「一键安装虚拟声卡」。
（首页出现「虚拟声卡现在用不了」那个黄色横幅时，旁边的按钮就是它。）

---

## 九、开源协议

**MIT**，见 [`LICENSE`](LICENSE)。个人使用、学习、修改、二次分发都没问题。

> **商用**（对外销售、集成进商业产品、以它提供商业服务）请先联系作者获得授权。
> 这是作者的一个请求，不是 MIT 的附加条款 —— MIT 的授权条件保持不变。

打包分发的第三方组件许可见 [`THIRD-PARTY-NOTICES.md`](THIRD-PARTY-NOTICES.md)。

---

## 如果它好用

这个项目从第一行代码到打包都是一个人写的，**没有广告、没有埋点、不收集任何数据**。
如果它帮你省了时间，给个 ⭐ 就是最实在的支持 —— 也能让更多拿着同一个遥控器、
正在忍受"来回切窗口"的人搜到它。

装的过程中卡住了、或者你的遥控器/输入法有不一样的表现，欢迎开 issue 说一声
（把日志页那几行贴上就行，排障全靠它）。
