# 遥控器客户端（VibeMote）

> 🌐 **中文** | [English](说明.en.md)

把 **Google Chromecast / Google TV 蓝牙遥控器**变成 Windows 上的说话与快捷键设备。

| 遥控器按键 | 默认效果 |
|---|---|
| **语音键**（带话筒那个） | **按住说话** —— 遥控器麦克风 → 虚拟声卡 → 输入法出字 |
| 上 / 下 | 鼠标滚轮上 / 下（**按住不放连续滚**，轻点滚一格） |
| 左 / 右 | `Ctrl+Shift+[` / `Ctrl+Shift+]` |
| OK / 返回 / 主页 / 电源 / 输入源 / 静音 / YouTube / NETFLIX / 音量± | 见界面「按键映射」页，随便改 |

---

## 一、这个版本干净在哪

- ✅ **不装驱动**（没有虚拟 HID、没有内核过滤驱动）
- ✅ **语音部分不需要管理员权限**。按键映射（可选）要**一次**提权（Frida 注入蓝牙 HID 驱动宿主），点一次「一键优化」后由最高权限计划任务代劳、之后不再弹；另外就是装虚拟声卡那一次 UAC。细节见第五节第 5 小节
- ✅ **不弹黑框**：单进程 + 全程隐藏窗口启动，自启动也不会闪任何窗口
- ✅ **不开测试模式、不关 Secure Boot、不碰系统文件**
- ✅ **不联网**（除了你自己点「安装依赖」那一下）

三条设计上的硬规矩，都是为了上面这几条：

1. **只有一个进程**。语音和按键都在同一个进程的线程里，不 Popen 子进程 ——
   所以架构上就不可能冒出控制台窗口。（唯一起子进程的地方是装依赖，那里显式隐藏窗口。）
2. **界面只监听 `127.0.0.1`**。安全，而且不会触发 Windows 防火墙弹窗
   （监听 `0.0.0.0` 才会弹，而且会反复弹）。
3. **配置以后端 `config.json` 为准**，前端不缓存、不反推。

---

## 二、快速开始（三步）

> **首页顶部有一张「首次准备」清单**，把下面这几件事的状态和按钮都摆在一起了
> （运行依赖 / 虚拟声卡 / 千问注入 / 开机自启 / 一键优化），照着从上到下点一遍就行 ——
> 不用自己去「设置」页里翻。**虚拟声卡是必需项**：不装它，按遥控器语音键不会有任何反应
> （遥控器麦克风没地方出声，输入法读不到）。

| 步 | 做什么 |
|---|---|
| 1 | 装 **Python 3.10+**（python.org 官方安装包，**务必勾选 Add python.exe to PATH**） |
| 2 | 双击 **`启动遥控器.vbs`** —— 界面会自动在浏览器打开（`http://127.0.0.1:8787/`） |
| 3 | 界面首页清单上点 **「全部安装」**（约 140MB，联网一次）—— 语音依赖 + 按键映射依赖 + 虚拟声卡一次装完；只想先用语音键，就点「只装语音依赖」（约 10MB） |

然后：

| 还想做什么 | 在哪 |
|---|---|
| 配对遥控器 | Windows 设置 → 蓝牙和其他设备 → 添加设备 → 同时按住遥控器 **返回 + 主页** 进配对 |
| 装虚拟声卡（语音必需的前置） | 首页清单「虚拟声卡」那一行点**一键安装**，或「设置 → 虚拟声卡」那张卡。**首次启动时程序会自动帮你把它拉起来**（只自动试一次）。⚠ 会弹一次 UAC，还要在安装器窗口里点一下「Install Driver」，**装完必须重启一次**（VB-Audio 官方要求） |
| 输入法里选麦克风 | 选 **`CABLE Output (VB-Audio Virtual Cable)`**。⚠ 只改**输入法里**这一处，**系统默认录音设备保持你原来的真实麦克风** —— 默认设备若设成虚拟声卡，微信语音/录音机这些应用也会拿到遥控器这一线 |
| 千问输入法用户 | 首页清单那一行点**打开**（程序首次启动也会自动帮你打开）；千问默认丢弃程序发的按键 |
| 开机自动跑 | 首页清单「开机自启」点**打开**（也可以去「设置 → 开机自启」）。**普通权限，不弹 UAC，开机后台静默启动** |
| **让 UAC 一次都不要再弹** | 首页清单「一键优化」点一下（弹一次管理员）—— 详见下面第二·补节 |
| **想彻底删干净 / 重装一遍** | 「设置 → 彻底卸载 / 清理」—— 详见第二·补二节 |

---

## 二·补、Frida 为什么会弹 UAC？怎么让它一次都不弹

**现象**：用着用着弹出「你要允许此应用对你的设备进行更改吗？」，标题附近能看到 `frida-helper`；
不管它或者点「否」，过一会儿又弹。

**原因**：按键映射靠 Frida 读蓝牙驱动里的报文。Frida 要注入一个不属于自己的进程时，
会**尝试注册一个临时服务**（叫 `frida-<PID>-x86_64`）来做这件事 —— 而注册系统服务
需要管理员，于是就有了 UAC。没批准或中途中断，这条服务就变成死残留。
反复重试 = 反复弹框 = 攒下一堆残留服务。

**两件事一起做才能根治**：

1. **程序侧**（已内置）：挂载失败改成**指数退避**，连撞 3 次权限失败就**停 5 分钟**，
   不再疯狂重试刷框。
2. **权限侧**：「设置 → **一键优化（弹一次管理员）**」。它会：
   - 清掉所有 `frida-*` 残留服务和助手进程；
   - 把开机自启从「普通权限的 Run 项」换成「**最高权限 + 登录触发**的计划任务」——
     开机由任务计划程序以管理员身份静默启动，Frida 要的权限都是现成的，
     **从此一次 UAC 都不会再弹**；
   - 删掉原来的 Run 项，避免两份同时启动抢蓝牙通道。

> 只想聊天说话、不需要方向键/OK 映射的话，**干脆不装 frida**（不点「安装按键映射依赖」），
> 那就永远不会有这个弹窗，也不会有下面这些麻烦。

**用按键映射要提前知道的三件事**（详细清单见 README 第五节第 5 小节）：

- **杀软可能误报/误隔离**：frida 的模块名在多家杀软的特征库里。离线包里它约 47MB —— 被静默隔离的话，
  表现就是「装了依赖但方向键/OK 没反应」。把程序目录加进白名单，再点一次「安装按键映射依赖」即可。
  **不需要**关 Defender、**不需要**开测试模式。
- **它靠的是未公开接口**：Windows 蓝牙 HID 驱动的一个未公开 IOCTL + 一个注册表路径，并锁定
  `frida >=17.18,<18`。系统或 frida 更新后可能**静默失效** —— 失效时语音照常，按键映射会停用并在日志说明。
- **注入的宿主同时服务你其他蓝牙键鼠**：我们只改被映射那几个键的报文（把原生动作抹掉），
  但万一这个宿主崩了，蓝牙键鼠会一起掉线。真遇到就重启客户端或拔插一下适配器。
- **受保护的游戏**：注入行为跟作弊器加载器同类，少数带内核反作弊的游戏可能起不来 —— 玩游戏前退出客户端最省事。

---

> **刚开机就能用**：开机那一刻 Windows 的音频服务 / 虚拟声卡常常比程序晚就绪。
> 本程序**不会因此罢工** —— 语音通道每 5 秒重试一次，声卡一就绪就自动接上，中途掉了也会重建。
> （只在启动那一刻找一次声卡的话，开机后按语音键会一直没声音。）

---

## 二·补二、清理 / 卸载（逐项勾选，想重装一遍时用）

「设置 → **清理 / 卸载（逐项勾选）**」。**默认什么都不勾、勾哪项删哪项**，没勾的一律不动。

可勾的 12 项（界面上就是这份，带 🔒 的会弹一次 UAC）。
**每一项后面还会标出「现在在不在／多大」** —— 比如轻包上「删除依赖（`.venv`）」会显示
「不存在」，不会让你对着不存在的项瞎勾：

| 项 | 说明 | 🔒 |
|---|---|---|
| 取消开机自启（计划任务） | 删除最高权限计划任务 `VibeMote` | ✓ |
| 取消开机自启（启动项） | 删除 HKCU Run 里的 `VibeMote` | |
| 结束客户端进程 | 语音桥接 + 按键映射进程 | |
| 结束 frida 助手进程 | `frida-helper-x86 / x86_64` | |
| 清理 frida 残留服务 | HKLM 里的 `frida-<pid>-x86*` | ✓ |
| 删除依赖（`.venv`） | 装过的 bleak / sounddevice / cffi / frida | |
| 删除 `__pycache__` | Python 字节码缓存 | |
| 删除 `python.txt` | 指定解释器的指向文件 | |
| 删除日志 | `elevate_setup.log` / `client.log` | |
| 删除 `config.json` | **你的按键映射会丢**，回到默认方案 | |
| 删除千问「允许注入」开关 | 删了千问会重新丢弃程序发的按键 | |
| 卸载虚拟声卡（VB-CABLE） | **系统级音频驱动**，见下面那段；默认不勾 | ✓ |
| 清除虚拟声卡残留（注册表 / 驱动包） | **装/卸卡住时的修复动作**，见下面那段；默认不勾 | ✓ |

三个辅助按钮：**全不选** / **选「推荐清理」**（上面 9 项，保留配置、千问开关和虚拟声卡）/ **全选**。

> ⚠ **「卸载虚拟声卡」是清单里唯一一个"点完还要人动手 + 重启"的项**，所以它
> **默认不勾、也不在「推荐清理」里**：
> - 它是**系统级音频驱动**，OBS / 变声器 / 直播工具可能也在用它，删了它们就没声了；
> - 官方**没有静默卸载**：Windows「应用和功能」里那条 `UninstallString` 就是安装器本身、
>   不带任何参数，人必须在它窗口里点一下「**Remove Driver**」；
> - 按 VB-Audio 官方 readme，**卸完必须重启一次**系统才算卸完。
>
> **「清除虚拟声卡残留」** 是给"装也装不上、卸也卸不干净"准备的：注册表里显示装了、
> 设备却枚举不出来，在官方安装器里点 Install Driver 也没反应 —— 那就是残留把新安装挡住了。
> 它只删 VB-CABLE 自己的那几处（两个服务键、`HKLM\SOFTWARE\VB-Audio\Cable`、
> 「应用和功能」里那条、DriverStore 里的驱动包、`C:\Program Files\VB\CABLE`），
> **同属 VB-Audio 的 Voicemeeter 的键和目录一个都不碰**；清完**重启一次**再重装。
> 首页出现「虚拟声卡现在用不了」那个黄色横幅时，右边的按钮就是它。
>
> 勾了它、点开始清理之后：程序会弹一次 UAC 并**把官方卸载器拉起来**，
> 剩下的两下（点 Remove Driver、重启）得你自己来。日志里会逐条说明。
> 程序**不会**去核实"卸载成功没有" —— 你还没点完就报"没删掉"是误报。

> ⚠ **清单里故意没有「删除内置 Python 运行时（`runtime\`）」这一项。**
> 它是离线包自带的 Python（解压后 154MB），属于**程序本体**，不是"装出来的依赖"；
> 而界面上的「全部安装」装的是 pip 包，**装不回**一个 Python 发行版 ——
> 一旦删掉，这台机器就再也不是"免安装"的了，而且没法一键恢复。
> 所以清理工具一直保留它。
>
> **想彻底卸载整个程序？两步，顺序别反：**
> 1. 在这里点「**选「推荐清理」**」→ 开始清理（清掉开机自启 + frida 残留服务 + 依赖）；
> 2. 然后**直接删掉整个程序目录**。
>
> 程序是纯绿色部署，没有安装器、不写系统目录，所以第 2 步之后系统里不会有任何残留。

**执行顺序是固定的**（不按勾选顺序，顺序错了会失败）：

```
先断自启 → 再杀进程 → 再删服务/文件 → 最后结束自己
```

尤其是「先杀进程再删 `.venv`」—— Windows 上**正在运行的 `pythonw.exe` 是被锁住的**，
顺序反了就报「删不掉」。极端情况下（机器上只有包内这一个解释器）程序会自动安排一个后台命令，
等本进程退出后接着删（日志里会写「已安排删除 .venv…约 2 秒」），你不需要做任何事。

**只想重装依赖** → 点「选「推荐清理」」即可，`config.json` 会留着。
**想完全回到"没装过"** → 再手动勾上 `config.json` 和千问开关（或直接「全选」）。

命令行等价写法：

```bat
python uninstall.py --items=venv,pycache        :: 只删这两项
python uninstall.py --items=all                 :: 全删（含 config.json、千问开关和虚拟声卡；但**不会**动 runtime\）
python uninstall.py --items=task,venv --dry-run :: 只列清单，什么都不做
python uninstall.py --dry-run                   :: 一个都不勾 = 什么都不做
```

---

## 二·补三、依赖下载走哪个源？（国内没 VPN 也能装）

「安装依赖」**自动轮询国内镜像**，一个失败就换下一个，最后才试官方 PyPI。
每次会打印用的是哪个源，所以出问题你能一眼看到。

**这张表按可用性排的**（每个都验证过能下，不是照抄网上推荐）：

| 顺序 | 源 | 结果 | 备注 |
|---|---|---|---|
| 1 | 腾讯云 | ✓ 1.1s | 最快 |
| 2 | 中科大 USTC | ✓ 1.0s | frida 索引稍慢（8.6s） |
| 3 | 阿里云 | ✓ 1.4s | |
| 4 | 华为云 | ✓ 1.1s | |
| 5 | 清华 TUNA | ✗ 返回空索引 | 已降级到最后 |
| 6 | 官方 PyPI | ✓ 2.5s | 兜底（有代理/国外网络时最稳） |

> **没收录网易 163**：它的 PyPI 镜像早就停止同步了（bleak 只到 0.5.0），
> 收录它反而会装出老版本、报出莫名其妙的错。也别用网上那种"一次列 8 个源"的配置。

**全失败怎么办**（程序会把这三条打在日志里）：

1. 换个网络（手机热点）再点一次
2. 手动改源：编辑 `app.py` 顶部的 `PIP_MIRRORS` 表（一行一个源）
3. **纯离线**：见下一节的 `wheels\` 目录 —— 程序检测到它**会优先使用，完全不联网**

---

## 三、界面怎么用

左侧五个页面：

- **状态**：运行状态、遥控器地址、语音/按键通道是否就绪、依赖缺什么、启动/停止/重启
- **按键映射**：**顶部先选「预设方案」**（见第三节·补），下面按遥控器实物排布画的图。
  点一个键 → 右边选**映射类型** → 选目标键 → 保存
  - 映射类型：

    | 类型 | 行为 |
    |---|---|
    | `单键` / `组合键` | 按一下就走一次（按下立刻抬起） |
    | `按住不放` / `按住组合键` | 遥控器键按住期间保持按下，松开才抬起（输入法「按住说话」就靠它） |
    | `鼠标` | 按一下发一次鼠标动作 |
    | **`按住鼠标`** | 鼠标**按住不放**：滚轮＝**按住一直滚**（轻点滚一格）、左/中/右键＝按住不放（拖拽用），松开才抬 |
    | `文本` | 按一下输入一段文字 |

  - 上和下的默认就是 **`按住鼠标` + 滚轮** —— 轻点滚一格、按住一直滚，不用自己配
  - 不知道物理键对应界面上哪个？打开 **「按下遥控器键来定位」**，按一下遥控器，界面自动跳过去
  - 填不出目标？点 **「按键捕获」**：按一下物理键盘上的键，**或者滚一下滚轮、点一下鼠标键** ——
    抓到鼠标动作时会按当前"按一下 / 按住"自动选好类型（在「按住不放」状态下滚轮，直接得到"按住连续滚"）
  - 已映射的键会在遥控器按键下面直接显示它映射成了什么；按下去界面有动画和信号灯
- **语音**：语音键目标（按住 / 按一下 + 修饰键 + 主键）、输入法预设、测试发键
- **设置**：蓝牙地址 + 扫描、音频增益 / AGC / 内置麦克风直通、开机自启、环境体检、装依赖
- **日志**：实时日志，出问题先看这里

改完映射**立即生效**，不用重启。

---

## 三·补、预设方案：选「应用」+ 选「输入法」，一键配好整套

「按键映射」页顶部有两条下拉，选完点「应用这套预设」，14 个按键 + 语音键一次配好。
下面会**先把整套映射预览出来**再让你决定，不会偷偷改。

### 应用（决定 14 个按键怎么映射）

| 预设 | 依据 |
|---|---|
| **WorkBuddy** | **它自己的「设置 → 快捷键」页**（最准）：`Ctrl+[`/`Ctrl+]` 上一个/下一个任务、`Ctrl+N` 新建对话、`Ctrl+B` 左侧栏、`Ctrl+Shift+B` 右侧产物面板、`Ctrl+F` 对话内搜索、`Ctrl+,` 打开设置、`F11` 全屏、`Shift+Alt+W` 唤起主窗口 |
| **Codex / ChatGPT 客户端** | **OpenAI 官方 Commands 参考页（Windows）**：`Ctrl+Shift+[`/`]` 上一个/下一个聊天、`Ctrl+Alt+A` 下一个需要关注的聊天、`Ctrl+N` 新建聊天、`Ctrl+B` 侧边栏、`Ctrl+J` 底部面板、``Ctrl+` `` 终端、`Ctrl+P` 搜索文件、`Ctrl+=`/`Ctrl+-` 字号、`F11` 全屏 |
| **通用** | 只用各程序都常见的键（`Ctrl+PageUp/PageDown` 切标签、`Ctrl+T` 新标签、`Ctrl+L` 地址栏……） |

### ★ 审批的 Yes / No

YouTube / NETFLIX 两个键固定映射成**审批的同意 / 拒绝**：

- **ChatGPT / Codex 官方定义：核准请求 = `Enter`，拒绝请求 = `Esc`**
- 所以这两个键就是 `Enter` / `Esc`（和 OK / 返回 同键，等于多了两个"随手同意/拒绝"的大按钮）

> 如果你用的程序审批框认的是别的键（比如 `y` / `n` 或 `1` / `2`），
> 改 `profiles.py` 里的 `_approve_yes()` / `_approve_no()` 两个函数即可，一处生效。

### 输入法（决定语音键发什么）

| 输入法 | 触发键 | 形态 | 状态 |
|---|---|---|---|
| 千问输入法 | 右 Alt | 按住 | ✅ 可用（它自己的配置里写着 `windows.vk.165`） |
| 搜狗语音输入 | 右 Ctrl | 按住 | ✅ 可用（搜狗语音助手日志里写着长按键是 RightCtrl） |
| 微信输入法 | Ctrl + Win | 按住 | ⚠️ 微信不提供静默改键接口，改完要在它的设置里核对 |
| Windows 语音输入 | Win + H | **按一下** | ✅ 系统固定，且是开关式不是按住 |
| 豆包输入法 | 右 Alt | 按住 | ❌ 不行：豆包会拦程序注入的按键 |
| 自定义 | —— | —— | 保持你当前在「语音」页的设置 |

---

## 四、语音键要换成别的输入法

遥控器的语音键发什么键，取决于你用的输入法。**最快的做法是上面三·补里的预设下拉**
（选输入法即填好）；也可以手动在界面「语音」页设：

| 输入法 | 它的「按住说话」快捷键 | 状态 |
|---|---|---|
| **千问输入法** | 按住 **右 Alt** | ✅ 已验证（需要开那个「允许注入」开关） |
| **搜狗语音输入** | 按住 **右 Ctrl** | 照预设填；若它设置里是别的键，以它显示的为准 |
| **微信输入法** | 按住 **Ctrl + Win** | 预设已填；微信不提供静默改键，需在它自己设置里核对一致 |
| **Windows 语音输入** | 按一下 **Win + H** | 系统自带；**模式要选「按一下」** |
| **豆包输入法** | 按住 **右 Alt** | ⚠️ 已知不行：豆包会拦程序注入的按键 |
| 自定义 | 你自己定 | 给别的语音软件留的口子 |

---

## 五、拷到别的电脑 / U 盘部署

整个 `vibe-mote` 文件夹拷过去即可，**包体不到 1MB**（依赖装在目标机上）。

**方式 A：联网装依赖**（默认）
目标机双击 `启动遥控器.vbs` → 界面点「安装基础依赖」。

**方式 B：纯离线**（推荐给没网的机器）
在一台**有网且系统相同**的机器上把依赖下载成 wheel 包，塞进 `wheels\` 目录，再整个拷过去：

```bat
python -m pip download -r requirements.txt -d wheels
python -m pip download -r requirements-keys.txt -d wheels     :: 需要按键映射才下，约 130MB
```

之后目标机点「安装依赖」时，程序**自动检测到 `wheels\` 目录就会改走 `--no-index --find-links wheels`**，
全程离线安装。

**方式 C：指定一个现成的解释器**（机器上已有装了依赖的环境时最省事）
在程序目录放一个 `python.txt`，里面**只写一行**：`pythonw.exe` 的完整路径。
启动器会**最先**用它，找不到就自动往下走（`.venv` → `runtime` → 系统 Python），所以这个文件放错了也不会出事。

```
D:\某处\vibe-mote\python.txt
→ C:\Users\你\AppData\Local\Programs\Python\Python313\pythonw.exe
```

**方式 D：直接拿「离线全量包」（连 Python 都不装，最省事）**
`vibe-mote-offline-v1.zip`（约 61MB）里已经带好了 Python 运行时和全部依赖：

```
对方机器上：解压 → 双击「启动遥控器.vbs」→ 直接用
不需要装 Python、不需要联网（按键映射仍要那一次提权，见 README 第五节第 5 小节）
```

**方式 E：自己放一个 runtime**（已经有一份现成的 Python 环境时）
把 Python 的 embeddable 包解压成 `runtime\` 放在程序目录里，启动脚本会优先用它
（查找顺序：`python.txt` → `.venv\` → `runtime\` → 常见安装位置 → PATH）。
注意 embeddable 包**默认关掉 `site`**、而且**没有 pip**：要让依赖能被 import 到，得打开
`python313._pth` 里的 `import site` 并加上一行 `Lib\site-packages`，依赖用外部
`pip install --target runtime\Lib\site-packages` 预装进去。直接用现成的离线全量包就不用管这些。

> 关于体积的实话：`frida` 的 `_frida.pyd` 一个文件就 **127MB**，且不可裁剪 ——
> 按键映射（方向键/OK/音量这些）只能靠它。所以「全功能离线包」解压后约 200MB
> （压缩后 61MB）。只想要语音键的话，约 15MB 就够，**不用 frida**。

---

## 六、出问题怎么查

**永远先点「设置 → 环境体检」**，它会逐项告诉你哪一层断了。

| 现象 | 多半是 | 怎么办 |
|---|---|---|
| **一直弹 frida-helper 的 UAC** | Frida 为注入注册临时服务要管理员；反复重试就反复弹 | 「设置 → **一键优化（弹一次管理员）**」，一次解决；详见第二·补节 |
| **刚开机按语音键没声音** | 开机时音频服务/虚拟声卡还没就绪 | **等 5~10 秒**即可（程序每 5 秒重试，日志会写「等虚拟声卡就绪…」→ 之后自动接上） |
| **装虚拟声卡弹 `Unknown Error 1072`** | 那个服务卡在「**待删除**」状态（之前的清残留删了服务、没先停驱动）。它提示「请用管理员运行」是**误导** —— 你本来就是管理员 | **重启一次**，再点「一键安装虚拟声卡」；还不行就右键 `vbcable\VBCABLE_Setup_x64.exe` →「以管理员身份运行」→ 点 Install Driver |
| 界面打不开 | 客户端没在跑 / 端口被占 | 双击 `启动遥控器.vbs` 再试；端口在「设置」页能看到 |
| 日志刷 `缺少 ATVV 特征` | **有另一个客户端占着遥控器的蓝牙通道**，或者本进程会话残留 | 关掉其它客户端（含旧版），再点「重启」。日志会打印枚举到的服务表 |
| 日志刷 `连接超时` | 遥控器在休眠 | **按一下遥控器任意键**，3 秒内会自动连上 |
| 按语音键没反应，但日志有 `▼ 开始` | 输入法不认注入的按键 | 千问：点「打开千问『允许注入』」；豆包：不支持 |
| 语音键连 `▼ 开始` 都没有 | 蓝牙没连上 / 别的客户端占着 | 看状态页的「语音」通道；先「重启」 |
| 中文出字但没人说话也出字 | 输入法的麦克风选错了 | 选 `CABLE Output (VB-Audio Virtual Cable)` |
| 识别不连贯、像有回声、或一直有底噪 | 系统默认录音设备被设成了 `CABLE Output`（「内置麦克风直通」会跟着把这一路转回去） | 把系统默认录音设备改回真实麦克风：「设置 → 系统 → 声音 → 输入」。虚拟声卡只需要出现在**输入法自己的**麦克风设置里 |
| 方向键/OK 没反应 | 没装 frida / 挂载失败 | 状态页看「按键」那行；点「安装按键映射依赖」 |
| 音量键按了会同时调系统音量 | 理论上不会（映射的键会在驱动层被抹掉原生动作）；若发生说明该键**没映射** | 映射表里加上它就自动屏蔽了 |
| 开机没自动起 | 自启项没写成功 | 「设置」页把开关关掉再打开；日志会给结果 |
| **遥控器按键有用，但按住语音键没反应 / 日志一直刷「连接超时」** | 蓝牙 BLE 设备只接受**一个**连接，Windows 的 HID 有时会把那条唯一的连接攥住不放 → 遥控器从此不再广播，程序就永远连不上 | 「设置 → 诊断 → **重置蓝牙电台**」（弹一次管理员，期间蓝牙设备会短暂断开）；不行就把遥控器**电池取出 10 秒**再装回 |

---

## 七、目录里都有什么

| 文件 | 作用 |
|---|---|
| `启动遥控器.vbs` | **唯一入口**（纯 ASCII + CRLF，故意不写中文注释，见文件头说明） |
| `app.py` | 全部逻辑：界面服务、配置、两个角色的管理、自启、体检、装依赖 |
| `i18n.py` | 中英双语：后端产生的界面消息（依赖说明 / 预设方案 / 清理清单 / 体检 / 动作回复） |
| `voice.py` | 语音：ATVV 语音键 + 遥控器麦克风 → 虚拟声卡（纯 Python 音频，不依赖 numpy） |
| `keys.py` | 按键：Frida 旁路读遥控器 HID 报文 → 合成键盘/鼠标 |
| `tap.js` | Frida 脚本（按键映射用） |
| `web/index.html` | 界面（单文件，零外部资源，离线可用） |
| `config.json` | 配置（首次运行自动生成） |
| `profiles.py` | **预设方案表**：应用（WorkBuddy / Codex）× 输入法 → 整套映射。改键位就改这里 |
| `uninstall.py` | 清理 / 卸载脚本（界面「清理」按钮调它，也可以命令行单独跑） |
| `elevate_setup.py` | 一次性优化（清 frida 残留 + 装最高权限自启 + 用新代码重启客户端） |
| `python.txt` | **可选**：指定用哪个 `pythonw.exe`（一行路径）。没有就自动找 |
| `.venv/` | **自动生成**：「安装依赖」建的包内虚拟环境（不污染系统 Python） |
| `requirements.txt` / `requirements-keys.txt` | 基础依赖 / 按键映射依赖（frida） |
| `wheels/` | 放离线 wheel 包就会自动走离线安装（可选） |
| `runtime/` | 放 Python embeddable 运行时（可选，用于免装 Python 的部署） |

---

## 八、运行环境

Windows 10 / 11 x64 · Python 3.10 或更高 · bleak 3.0.2 · sounddevice 0.5.6 · frida 17.18 ·
VB-CABLE 4.5 · 千问输入法（可选）。
遥控器：Google TV（Chromecast）蓝牙遥控器。

## 九、开源

MIT（见 `LICENSE`）。个人使用、学习、修改、二次分发都没问题；
商用（对外销售、集成进商业产品、以它提供商业服务）请先联系作者获得授权。

打包分发的第三方组件许可见 `THIRD-PARTY-NOTICES.md`。
